{% extends 'base.html.twig' %}
{% block title %}Sign Up!{% endblock %}

{% block body %}
    <style>
        footer {
            position : absolute;
        }
    </style>
    <h1>Modifier vos données personnelles</h1>

    {% for message in app.session.flashBag.get('file_error') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}
    {{ form_start(form)}}

    <div id="dataset3">
        {{ form_row(form.image) }}
        {{ form_row(form.telephone) }}
        {{ form_row(form.gouvernorat) }}
    </div>

    {{ form_row(form.confirmer) }}
    {{ form_end(form) }}
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/bootstrap-form/bootstrap.css') }}">
    {{ parent() }}
    <script src="https://www.google.com/recaptcha/api.js"></script>
    <link rel="stylesheet" href="{{ asset('css/sign_up.css') }}">
{% endblock %}

{% block javascripts %}
{% endblock %}